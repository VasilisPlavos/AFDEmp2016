how to run:
Install Python3 (look http://www.python.org )
Install pip, setuptools, virtualenv (look http://flask.pocoo.org/docs/0.11/installation/#windows-easy-install )
Install flask, flask_wtf (pip install flask, pip install flask_wtf)
start run.bat
wait 5 seconds!
(if any error it will appear at the cmd)